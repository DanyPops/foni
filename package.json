{
  "name": "foni",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "scripts": {
    "test":      "vitest run",
    "test:watch": "vitest",
    "demo":      "FONI_PLAY=1 RVC_URL=http://127.0.0.1:5050 vitest run pipeline.e2e -t 'cheeki breeki'",
    "listen":    "FONI_PLAY=1 RVC_URL=http://127.0.0.1:5050 node scripts/listen.mjs"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
